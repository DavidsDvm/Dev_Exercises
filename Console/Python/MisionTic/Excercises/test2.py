"""
calculate the velocity of an object
"""

distancy = int(input("Ingrese la distancia del vehiculo (en metros): "))

time = float(input("Ingrese el tiempo del vehiculo (en segundos): "))

velocity = distancy / time

print ("La velocidad es :", velocity)